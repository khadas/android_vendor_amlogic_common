<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MultiController" parent="@android:style/Theme.Holo.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MultiController" parent="Base.Theme.MultiController" />
    <style name="Theme.MultiController.Control" parent="Theme.MultiController">
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
