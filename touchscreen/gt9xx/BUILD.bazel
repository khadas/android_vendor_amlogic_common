load("//build/kernel/kleaf:kernel.bzl", "kernel_module")

kernel_module(
    name = "gt9xx",
    srcs = glob(
        ["**"],
        exclude = [
            "BUILD.bazel",
            "**/*.bzl",
            "**/*.mk",
            "**/*.bp",
        ],
    ),
    outs = [
        "aml_gt9xx.ko",
    ],
    kernel_build = "//common:amlogic",
    visibility = [
        "//common:__pkg__",
    ],
)
